"""Core harness internals: messages, events, the loop, tools, capabilities.

Most applications import from the top-level :mod:`agento` package instead. This
subpackage is the place to look when you are extending agento — writing a
capability, a tool set, or an LLM adapter.
"""
